using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GaussNameChange : MonoBehaviour
{
    // Start is called before the first frame update
    public Turret turscript;
    void Start()
    {
        turscript.name = "GaussGun";
    }

}
